MEMORY {
    ZP:       file = "", define = yes, start = $0000, size = $0100;
    RAM:      file = %O, define = yes, start = $0300, size = $FC00;
}
SEGMENTS {
    ZERO:     load = ZP,     type = zp;
    CODE:     load = RAM,    type = rw;
}
